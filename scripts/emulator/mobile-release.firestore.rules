// Local demo-project concurrency fixture only. NEVER deploy these test rules.
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /clubs/emulator_handover/{document=**} {
      allow read, write: if true;
    }
  }
}
