# COM-046 — regressie badges en pushnavigatie

## Intake

- Report-ID: `eOxrQ34oUgpmmjj3Jrg4`
- Go-revisie: `c16cc2f2-fb70-4349-b5a3-4dc7106cc110`
- Oorspronkelijke delivery-run: `fix-20260907-COM046-full-sweep`
- Returned-ticket-run: `fix-20260907-COM046-notification-round3`
- App in ticket: CalyCompta; getroffen client: CalyMob
- Classificatie: regressie in badgeberekening en pushcontract
- Uitvoerdatum: 2026-09-07
- Release: geen version bump, build-upload, storehandeling of deploy

## Voor

- Event-, chat- en communicatiebadges konden ontbreken of blijven staan.
- Eventberichten werden alleen voor maximaal tien recente open events geteld.
- Sessieberichten werden alleen voor vijf gepubliceerde sessies geteld en alle
  niveauchats van een sessie deelden dezelfde lokale read-key.
- Pushrouting verwachtte uitsluitend snake_case-ID's en een tap werd al vóór de
  navigatiepoging als afgehandeld gemarkeerd.

## Analyse

De client combineert lokale leestijdstippen met Firestore count-queries. De
historische caps maakten die bron onvolledig voor oudere of gesloten, maar nog
relevante gesprekken. De gedeelde niveau-key kon een ander niveau onterecht als
gelezen markeren. Verschillende zenders gebruikten daarnaast zowel snake_case
als camelCase, terwijl de mobiele parser slechts één vorm accepteerde.

De eerder geïsoleerde fix `057aae1` is opnieuw vergeleken met actuele
`origin/main` (`97968cf`) en conflictvrij overgenomen. De reeds op main aanwezige
waitlist-promotieroute blijft behouden.

## Fix

- Tel eventberichten voor alle telbare inschrijvingen van de gebruiker,
  inclusief gesloten events; `canceled` en `waitlisted` tellen niet mee.
- Valideer dat collection-groupresultaten werkelijk onder de gevraagde club en
  een operation-pad vallen.
- Tel alle gepubliceerde sessies en gebruik voor niveauchats een read-key en
  query per niveau.
- Gebruik dezelfde niveaugebonden read-key wanneer een sessiechat wordt geopend.
- Normaliseer snake_case- en camelCase-payloadvelden in één requestmodel.
- Valideer vereiste bestemmings-ID's vóór enqueue; bewaar een cold-startrequest
  wanneer de navigator nog niet klaar is en markeer pas na een navigatiepoging.

## Geautomatiseerde dekking

- Payloadmapping is gelijk voor foreground, background en cold start.
- Snake_case- en camelCase-ID's, onvolledige bestemmingen en bestaande
  notificatietypes zijn afgedekt.
- Queue-deduplicatie tijdens pending én actieve navigatie, retry na ontbrekende
  navigator en verval van de duplicate-window zijn afgedekt.
- Twee verschillende foregroundberichten met dezelfde sessie maar een ander
  `message_id` worden allebei gequeued; een herhaalde callback voor hetzelfde
  bericht blijft onderdrukt, ook zolang de eerste navigatie nog bezig is.
- Registratiestatussen en onafhankelijke sessieniveau-read-keys zijn afgedekt.

## Returned review — delivery revision 2

De onafhankelijke review van commit `452d90c` reproduceerde dat lokale
foreground-taps geen `RemoteMessage.messageId` meekrijgen. Hoewel de FCM-payload
wel het Firestore-veld `message_id` bevat, gebruikte de deduplicatiesleutel dat
veld niet. Daardoor kregen twee verschillende berichten binnen dezelfde sessie
dezelfde fallback-sleutel en werd het tweede bericht onterecht onderdrukt.

De round-3-correctie gebruikt `message_id`/`messageId` als stabiele fallback
wanneer de transport-ID ontbreekt. De fallback op bestemmings-ID blijft alleen
bestaan voor notificatietypes zonder bericht-ID. Dit document en de nieuwe
delivery bij PR #67 vervangen de verouderde revision-1-dossierreferentie in
CalyCompta PR #36; die PR hoort niet bij de nieuwe delivery.

## Validatie

- Gerichte notificatienavigatie- en unread-regressietests: 33/33 geslaagd.
- Volledige Flutter-suite: 508 geslaagd, één bestaande emulatorafhankelijke
  test overgeslagen.
- Gerichte analyzer op de round-3-productieservice: geen issues.
- Projectbrede analyzer: bestaande repositorybaseline van 954 waarschuwingen
  en info-meldingen; geen analyzererrors en geen issues in de gewijzigde
  productie-Dartbestanden.
- Android debug APK-build: geslaagd.
- `git diff --check`: geslaagd; de functionele hunks zijn op de bestaande
  bestandsformattering gehouden om ruis buiten COM-046 te vermijden.

## Risico en handmatige review

> **SUPERSEDED (2026-09-25) by unread cursor v1 — see the addendum below.**
> The LocalReadTracker/count-query expansion in this delivery is retained as
> history but is not the definitive cross-device unread design.

- Bij veel historische events of gepubliceerde sessies ontstaan meer parallelle
  count-queries; de bestaande timeout van acht seconden per query blijft actief.
- Server-side `unread_counts` is niet gemigreerd of herontworpen. Controleer bij
  release expliciet de synchronisatie met het app-icoon op Android en iOS.
- Test foreground/background/cold-startpushes en badge verhogen/openen/lezen/
  resetten voor announcements, eventchat, teamchat en meerdere sessieniveaus.
- Er is geen datamigratie: bestaande accueil- en encadrants-keys blijven gelijk;
  niveauchats krijgen voortaan een specifiekere lokale key.

## Delivery

- Branch: `codex/bug-COM-046-notification-regression-v2`
- Draft PR: `https://github.com/Jan-H2M/CalyMob/pull/67`
- Worktree: `/private/tmp/calymob-com046-v2`
- Geen version bump, release, live-datawijziging, merge of deploy.

---

## Addendum — definitive unread cursor v1 (2026-09-25)

### Design decisions (Phase 2)

Phase 2 is local client infrastructure only. A new `ReadStateService` owns the
server-timestamp cursor schema; `CursorUnreadCountService` derives the confirmed
four-category formula with bounded queries and the seven-calendar-day Brussels
event rule. It is controlled by `off|shadow|on`: only `on` changes displayed
counts, and it never writes `unread_counts`; shadow logs comparison data only.
No navigation/read acknowledgement trigger from COM-046 was changed yet, so this
does not alter live screen behavior while the flag remains OFF.

COM-046 removed historical query caps but left the local read tracker and legacy
counter model in place. Cursor v1 replaces both with self-owned, server-timestamp
cursor documents at `clubs/{clubId}/members/{uid}/read_state`: announcement
cursor; event conversation cursors; team-channel cursors; session-chat cursors;
and optional per-section global cursors. The effective cursor is `max(global,
scope)`, so an acknowledgement is one bounded write rather than N `read_by`
message writes. `unread_counts` increment/decrement logic is not authoritative
after cutover; Functions will calculate the same total as tiles and push an
exact APNs badge, including zero.

Confirmed semantics: app icon = Événements + Communication; Communication =
announcements + team + session; Événements = event messages; event messages are
eligible until `date_fin + 7` Europe/Brussels calendar days. Opening a tile does
not acknowledge a section; opening a conversation does, with explicit *Tout
marquer comme lu* actions. All non-soft-deleted announcements count for all
members. Feature flag is OFF by default; old apps temporarily coexist before a
minimum-version cutover. Phase 1 is local-only on `feat/unread-cursor-v1-phase1`
and not deployed. See `../../../outputs/calymob-unread-definitive-plan_2026-09-24.md`.
