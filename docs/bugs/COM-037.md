# COM-037 — alle notificatiebadges in één keer wissen

## Intake

- App in ticket: CalyCompta; gevraagde functie: CalyMob
- Prioriteit/status: gênant; En cours bij audit
- Melder/tijd: clubbeheerder; 2026-04-07
- Route/versie/device: `/accueil`; webformulier in ontwikkelversie
- Bewijs: titel en beschrijving; geen screenshot
- Classificatie: verbetering, historisch al geïmplementeerd
- Aanname/vertrouwen: hoog; de gevraagde Franse knop en volledige cascade
  staan expliciet op de huidige CalyMob-main.

## Voor

- Reproductie: niet opnieuw uitgevoerd op een fysiek toestel.
- Verwacht: één actie in Instellingen wist alle rode notificatiepunten.
- Historisch werkelijk gedrag: er was geen centrale actie in de gemelde versie.
- Gereproduceerd: afgeleid uit Git-historiek en actuele code.
- Baseline: de huidige implementatie bestond al, maar een rechtstreekse test van
  de globale `markAllAsRead`-baseline ontbrak.

## Analyse

- Getroffen pad: `SettingsScreen` → `LocalReadTracker` →
  `UnreadCountProvider`, Firestore `unread_counts` en lokale notificaties.
- Oorzaak: vóór commit `70a0b39e` konden gesprekken alleen afzonderlijk als
  gelezen worden gemarkeerd.
- Scope/risico: alleen leesstatus en badges; berichten zelf worden niet gewist.
- Gerelateerd: MOB-005 documenteert de betekenis van een afzonderlijke badge;
  COM-046 behandelt bredere badge- en pushnavigatieregressies.

## Fix

- Branch: `codex/bug-com-037-mark-all-read`
- Historische functionele fix: commit `70a0b39e535f3732d4959ae940b333e3bbf13751`
  van 2026-04-08.
- Nieuw gewijzigd: dit dossier en een regressietest in
  `test/services/unread_count_service_test.dart`.
- Gedrag: de knop `Marquer tout comme lu` vraagt bevestiging, zet een globale
  leesbaseline, reset de categorie- en totaaltellers, wist OS-badges en pending
  notificaties en vernieuwt de UI.
- Regressietest: bevestigt dat de globale baseline zowel bestaande als nooit
  geopende gesprekken afdekt.

## Validatie

- Gerichte tests: `unread_count_service_test.dart`, 13/13 geslaagd.
- Volledige suite: `flutter test`, 376/376 geslaagd.
- Build/analyzer/lint: de nieuwe test volgt bewust de bestaande bestandsstijl;
  er is geen productiecode gewijzigd.
- Handmatige QA: reviewstappen hieronder; geen fysiek toestel beschikbaar.
- Baselinefouten: geen.

## Reviewstappen

1. Zorg dat meerdere rode badges zichtbaar zijn.
2. Open `Mon Profil` → Instellingen.
3. Kies `Marquer tout comme lu` en bevestig.
4. Controleer dat de appbadge, pending notificaties en rode tellers verdwijnen.
5. Plaats daarna een nieuw bericht en controleer dat dit opnieuw als ongelezen
   verschijnt.

## Na

- Bewijs: code, Git-historiek en regressietest.
- Restrisico: OS-specifiek wissen van het badge-icoon is niet op een fysiek
  Android- of iOS-toestel herhaald.
- PR/merge: wordt na groene validatie ingevuld.
- Release: de functionele fix zit al in uitgebrachte builds; alleen test en
  documentatie worden toegevoegd, dus geen nieuwe mobiele releasewachtrij.
- Ticketovergang: na gestructureerde oplevering naar Review, nooit automatisch
  naar Résolu.
