# COM-043 — zoekfunctie werkt niet

## Intake
- App: CalyCompta, backend in CalyMob Cloud Functions
- Priority/status: gênant / nouveau
- Reporter/time: Jan ANDRIESSENS, 09/08/2026 20:47
- Route/version/device: `/parametres/sites-plongee/reference`, manual report
- Evidence: ticket screenshot `SCR-20260809-sagr.png`
- Classification: bug
- Assumptions/confidence: `Hur` is intended to find reference names beginning with `Hur`, high confidence

## Before
- Reproduction steps: open Base de référence and search for `Hur`
- Expected: matching SSI reference sites such as names beginning with `Hur` are shown
- Actual: the result list is empty
- Reproduced: yes, on production on 10/08/2026
- Baseline test: no prefix-query regression coverage existed

## Analysis
- Affected path: `functions/src/referenceData/resolveReferenceDiveSiteLocation.js`
- Root cause: candidate retrieval queried `search_tokens` with `array-contains`, which only matches complete tokens; the later substring filter never received candidates for a partial token
- Scope and risks: administrator-only reference-site search; no writes or member-visible data
- Duplicates/related tickets: COM-048 concerns search reliability in the separate dive-location catalogue

## Fix
- Branch: `codex/bug-com-043-reference-search`
- Changed files: reference-data Function, its Jest test, this dossier
- Behavior: merge normalized-name prefix candidates with exact-token candidates before applying the existing filter
- Regression test: `Hur` produces the Firestore prefix range `hur` through `hur\uf8ff` and matches `Hurghada`

## Validation
- Targeted tests: 1 suite, 6/6 passed
- Full suite: 18 suites, 108/108 passed
- Build/analyzer/lint: Function module load check and `git diff --check` passed
- Manual or browser QA: production reproduction and post-deployment verification passed; `Hur` returns Huraa Etere, Hurawali Kandu and Hurricane Bay, and `1000` returns 1000 Rocks
- Baseline failures: none in the Function suite; `npm ci` reports 27 dependency audit findings (2 low, 14 moderate, 7 high, 4 critical), unchanged by this patch

## After
- Evidence: production verification on 10/08/2026 after the isolated Function deployment
- Residual risk: prefix search covers the beginning of normalized names; arbitrary mid-word Firestore substring search remains intentionally unsupported
- PR: CalyMob #15
- Merge commit: `e7abf6ab47d023302cd0285f904a457b5b4a638a`
- CalyCompta deployment or CalyMob release-queue entry: `listReferenceDiveSites` successfully deployed to `europe-west1`; no mobile release required
- Ticket transition: technically resolved after production verification
